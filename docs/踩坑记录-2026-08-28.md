# 踩坑记录 2026-08-28（LOGO 上线 + 信任套件）

> 目的：下次同类任务（小改动 + 推 GitHub Pages 上线）控制在 10 分钟内。

## 这次时间都耗在哪

| 坑 | 现象 | 根因 | 下次怎么做 | 省时 |
|---|---|---|---|---|
| 本地服务端口被占 | EADDRINUSE 反复失败 | 残留 node 进程没杀 | 动手前先 `netstat -ano \| grep :端口`，有就先 taskkill | ★★ |
| Chromium 截图反复失败 | spawn ENOENT / 卡死超时 | 带空格路径 + 反斜杠转义 + headless 兼容性 | **别再用本地 Chromium 截图**。改 PIL 合成或让用户出截图 | ★★★★★ |
| Python 正则脚本改 JS | `[\s\S]` 被转义成 `[sS]`，MISS 报错 | bash heredoc 把 `\s` 吃了 | 改 JS/HTML 一律用 edit/apply_patch 工具，不走 python -c/heredoc | ★★★★ |
| 单测 "$ 多行陷阱" | extractKeycard 只取到 1 项 | 正则 `(A\|$)` 在 m 模式下 `$` 匹配每行行尾，非贪婪提前停 | 写正则先想 m 模式下 `$` 语义；能用字符串 split 就不用正则 | ★★ |
| 根 logo.png 假存在 | 脚本打印 OUTPUT 但文件不在 | cp 目标目录不存在，报错被 `2>/dev/null` 吞了 | 脚本结尾逐文件 `test -f` 校验，失败立即 exit 1 | ★★★ |
| 推送假成功 | 打印 PUSH_OK 但远端没更新 | `git push \|\| echo PUSH_OK` 把失败也打成功字样 | push 后必须 `git ls-remote` 对比 SHA 才算数 | ★★★★ |
| Pages 部署误判 404 | 空转轮询 8 分钟 | 第一笔 push 网络中断实际没上去；另一处 grep 二进制 zip 误报 | 404 时先查 `git ls-remote`，push 没上去就重推，别傻等 | ★★★★ |
| favicon 假失败 | grep 说桌面 zip 缺规则 | 对二进制 zip 直接 grep 文本 | 验 zip 要先解包再比对（unzip -p 或解压 diff），不 grep 原始字节 | ★★ |

## 铁律（下次直接执行）

1. **验证以"最终效果"为准**：远端 SHA、HTTP 200、文件存在于磁盘——不看脚本自打印的"成功"字样。
2. **推送后第一步：`git ls-remote` 对比本地/远端 SHA**，一致才进入等 Pages 环节。
3. **本地预览一律 PIL 合成**，不再调本地 Chromium（环境有路径空格+兼容问题）。
4. 多文件副本（根/app/gh-pages 三份）改完先 `grep -c` 数齐再推送。
5. 任何脚本静默删错误（2>/dev/null）都要配事后存在性校验。

## 本次成果（已全部上线）

- 顶栏 + favicon 换海诚 LOGO（commit aa875ff + b7ee154）
- 信任套件四件套：👍👎反馈、⚡要点卡、角标溯源、Prompt 诚实兜底（c609f32）
- 修复上线版漏同步"第二梯队"问题

## 遗留待办

- [ ] 云函数 Prompt 更新：**用户手动**上传桌面 `scf-deploy.zip` 到腾讯云 `zhidu-qa-proxy`（诚实兜底功能需此步生效）

---

# 主题切换翻车复盘（同日晚）

> 功能本身 30 分钟写完，bug 排查 + 验证方式重构又花了一轮。核心教训就一条：**验证手段必须覆盖用户的真实交互路径**。

## 这次栽在哪

| 坑 | 现象 | 根因 | 下次怎么做 |
|---|---|---|---|
| **浮层遮挡没测出** ⭐核心 | 用户点「🎨 主题」毫无反应，我这边测试全绿 | header/main/footer 三层同为 z-index:1，main 靠后盖住下拉菜单；程序化 click() 不走视觉命中，测不出遮挡 | 浮层/弹出类改动，**必须做 elementFromPoint 命中测试**，程序化 clickocratic 不算数 |
| 截图验证走直链捷径 | 三主题截图都"过"了 | 用 `?theme=` 直链参数拍图，绕过了"点菜单"这条真实路径 | 截图前先问：用户实际怎么触发这个效果？截图必须复刻用户路径 |
| iframe 本地测试被 file:// 拦 | contentDocument 为 null | 浏览器跨 file 协议 iframe 限制 | 本地 DOM 测试用「拷贝 index.html + 尾部追加自测脚本」模式（scripts/_ztest.py 可复用） |
| 旧版复现踩了 /c/tmp 陷阱 | mkdir 了 /c/tmp 但 node 读 C:\tmp | Git Bash 的 /c 与 Windows 路径混淆 | 临时文件统一放项目 preview/ 下，别放系统根 |

## 验证层级（今后 UI 改动的标准流程）

1. **语法层**：抽出内联 JS 做 node --check
2. **逻辑层**：程序化点击（click() 流）
3. **视觉层** ⭐：elementFromPoint 命中测试——确认鼠标真会点到目标元素
4. **用户层**：截图/实测复刻用户操作路径，不是走捷径参数

这次 1、2 层全过、3、4 层缺失，恰好漏掉的正是出问题的层。

## 修复与上线

- commit a4c96be：header z-index 提到 20
- 命中测试前后对比：旧版戳到 main（bug 实锤）→ 新版戳到菜单 BUTTON ✅
